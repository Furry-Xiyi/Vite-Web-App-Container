using Windows.UI.Xaml.Controls;

namespace WinUIonWebUWP.Dialogs
{
    public sealed partial class DeleteCssRuleDialog : ContentDialog
    {
        public DeleteCssRuleDialog()
        {
            this.InitializeComponent();
        }
    }
}
